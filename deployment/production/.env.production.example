# ====================================
# VEXUS - CONFIGURACIÓN DE PRODUCCIÓN
# ====================================
# INSTRUCCIONES:
# 1. Copiar este archivo como .env.production
# 2. Reemplazar TODOS los valores con configuraciones reales y seguras
# 3. NUNCA subir este archivo al repositorio
# 4. Usar variables de entorno del servidor/contenedor en producción

# === BASE DE DATOS ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CAMBIAR_ESTA_CONTRASEÑA_SUPER_SEGURA
POSTGRES_DB=vexus_db
POSTGRES_PORT=5432

# URL completa (usada por el backend)
DATABASE_URL=postgresql://postgres:CAMBIAR_ESTA_CONTRASEÑA_SUPER_SEGURA@db:5432/vexus_db
DB_POOL_MIN_SIZE=10
DB_POOL_MAX_SIZE=50

# === SEGURIDAD ===
# IMPORTANTE: Generar una SECRET_KEY segura usando:
# python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CAMBIAR_ESTA_CLAVE_SECRETA_SUPER_LARGA_Y_ALEATORIA_MINIMO_32_CARACTERES
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# === CORS ===
# Especificar los dominios permitidos separados por comas (SIN espacios)
# Ejemplo: https://vexus.com,https://www.vexus.com,https://app.vexus.com
ALLOWED_ORIGINS=https://tu-dominio.com

# === PUERTOS ===
BACKEND_PORT=8000
FRONTEND_PORT=80

# === EMAIL (configurar según tu proveedor) ===
# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# Otros proveedores: SendGrid, Mailgun, AWS SES, etc.
SMTP_HOST=smtp.tu-proveedor.com
SMTP_PORT=587
SMTP_USER=tu-email@dominio.com
SMTP_PASSWORD=tu-contraseña-de-app
EMAIL_FROM=noreply@vexus.com

# === APLICACIÓN ===
PROJECT_NAME=Vexus API
VERSION=1.0.0
API_V1_PREFIX=/api/v1
ENVIRONMENT=production
DEBUG=False

# === OPCIONAL: Configuración adicional ===
# SENTRY_DSN=https://your-sentry-dsn  # Para monitoreo de errores
# REDIS_URL=redis://redis:6379/0      # Para caché/sesiones
