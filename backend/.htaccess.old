# ==================================
# NEATECH - Configuración Passenger
# ==================================

# Habilitar Passenger para Python
PassengerEnabled on
PassengerAppType wsgi
PassengerStartupFile passenger_wsgi.py

# Ruta al intérprete de Python
# Neatech usa Python del sistema, NO necesitas venv si no hay SSH
PassengerPython /usr/bin/python3

# Configuración de la aplicación
PassengerAppRoot /home/grupovex/web/grupovexus.com/api

# Performance
PassengerMinInstances 1
PassengerMaxPoolSize 6

# Headers CORS (por si acaso)
Header always set Access-Control-Allow-Origin "*"
Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH"
Header always set Access-Control-Allow-Headers "Content-Type, Authorization"

# Logs
PassengerLogLevel 3
PassengerAppLogFile /home/grupovex/logs/passenger_app.log

# No mostrar archivos Python
<FilesMatch "\.(py|pyc|pyo)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Permitir passenger_wsgi.py
<Files "passenger_wsgi.py">
    Allow from all
</Files>
