# ====================================
# VEXUS - CONFIGURACIÓN EJEMPLO
# ====================================
# INSTRUCCIONES:
# 1. Copiar este archivo como .env
# 2. Reemplazar los valores con tus credenciales reales
# 3. NUNCA subir el archivo .env al repositorio

# === BASE DE DATOS ===
DATABASE_URL=postgresql://postgres:TU_CONTRASEÑA@localhost:5432/vexus_db
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20

# === SEGURIDAD ===
SECRET_KEY=cambiar-esta-clave-secreta-en-produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === CORS ===
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5500

# === EMAIL (opcional - para funcionalidad futura) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASSWORD=tu-contraseña-de-app
# EMAIL_FROM=noreply@vexus.com

# === APLICACIÓN ===
PROJECT_NAME=Vexus API
VERSION=1.0.0
API_V1_PREFIX=/api/v1
ENVIRONMENT=development
DEBUG=True