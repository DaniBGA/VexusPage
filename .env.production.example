# ====================================
# VEXUS - VARIABLES DE ENTORNO DE PRODUCCIÓN
# ====================================
# Copia este archivo como .env.production y configura tus valores reales

# === BASE DE DATOS ===
POSTGRES_DB=vexus_db
POSTGRES_USER=vexus_admin
POSTGRES_PASSWORD=CAMBIA_ESTA_CONTRASEÑA_SEGURA

# === BACKEND ===
PROJECT_NAME="Vexus API"
VERSION="1.0.0"
ENVIRONMENT=production
DEBUG=false

# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERA_UNA_CLAVE_SECRETA_AQUI

# Pool de conexiones
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_CONNECT_TIMEOUT=10

# Tiempo de expiración del token JWT (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === CORS ===
# Separar múltiples orígenes con comas
ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com

# === EMAIL (Gmail SMTP) ===
# Configuración Gmail SMTP
# 1. Habilita verificación en 2 pasos en tu cuenta de Gmail
# 2. Genera un App Password en: https://myaccount.google.com/apppasswords
# 3. Usa smtp.gmail.com:587 con TLS/STARTTLS
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_app_password_de_16_caracteres
EMAIL_FROM=tu_email@gmail.com

# === FRONTEND ===
FRONTEND_URL=https://tudominio.com
API_URL=https://api.tudominio.com

# === AWS LIGHTSAIL (Opcional) ===
# Si usas RDS en lugar de contenedor PostgreSQL
# DATABASE_URL=postgresql://usuario:contraseña@endpoint-rds.region.rds.amazonaws.com:5432/vexus_db
